---

과제 3: 부서 → 구성원 → KPI 조회 기능

목표

부서를 선택하면 해당 부서의 구성원이 나오고, 구성원을 선택하면 해당 구성원의 KPI가 나오는 기능 구현

현재 DB 구조

deptList (부서)
├── DEPT001 개발팀
├── DEPT002 영업팀
└── DEPT003 인사팀

userList (구성원)
├── USER001 김개발 → DEPT001
├── USER002 이프론트 → DEPT001
├── USER003 박영업 → DEPT002
├── USER004 최세일 → DEPT002
├── USER005 정인사 → DEPT003
└── USER006 강채용 → DEPT003

kpiList (KPI)
├── 2024년 데이터 (6명 × 2~3개씩)
└── 2023년 데이터 (6명 × 2개씩)

만들어야 할 것

1. API 3개
   | API | URL | 파라미터 | 반환 |
   |-------------|----------------|--------------|-----------------------------|
   | 부서 목록 | /api/dept/list | 없음 | deptList 전체 |
   | 구성원 목록 | /api/user/list | deptId | 해당 부서 구성원만 |
   | KPI 목록 | /api/kpi/list | userId, year | 해당 구성원의 해당 연도 KPI |

2. useKpiApi.ts 확장

- fetchDeptList() 추가
- fetchUserList(deptId) 추가
- 기존 fetchKpiList에 userId 파라미터 추가

3. app.vue 수정
   [부서 선택 ▼] → [구성원 선택 ▼] → [연도 선택 ▼] → [조회]

힌트

구성원 필터링 (server/api/user/list.post.ts)
const filtered = userList.filter(user => user.deptId === deptId)

KPI 필터링 (server/api/kpi/list.post.ts)
const filtered = kpiList.filter(kpi =>
kpi.userId === userId && kpi.year === year
)

---

완성하면 Network 탭에서 확인해보세요!
