과제 4 (최종)

목표

- 유저 클릭 시 → 해당 유저의 부모 1단계부터 트리 표시

---

로직

// selectedUser 기준으로 부모 찾기
const rootUsers = computed(() => {
// 1. 선택된 유저 찾기
const currentUser = userList.value.find(u => u.userId === selectedUser.value)

    // 2. 부모 ID
    const parentId = currentUser?.parentId

    // 3. 부모 없으면 본인이 루트
    if (!parentId) {
      return userList.value.filter(u => u.userId === selectedUser.value)
    }

    // 4. 부모부터 시작
    return userList.value.filter(u => u.userId === parentId)

})

---

결과

최세운 클릭:
홍덕기 (그룹장)
└── 최세운 ← active
├── 이찬용
└── 박영수

이찬용 클릭:
최세운 (본부장)
└── 이찬용 ← active
├── 김한나
└── 김디디

김한나 클릭:
이찬용 (팀장)
├── 김한나 ← active
└── 김디디

---

핵심

selectedUser가 바뀌면 → rootUsers가 자동으로 재계산됨 (computed)
