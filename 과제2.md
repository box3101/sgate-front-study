과제 1: Network 탭으로 API 분석하기

1. npm run dev로 서버 실행
2. F12 → Network → Fetch/XHR
3. 2024년 조회 → 2023년 조회
4. 각각 아래 내용 정리:
   - Request URL:
   - Request Method:
   - Payload (보낸 값):
   - Response (받은 값):

---

과제 2: 삭제 API 만들기

1. server/api/kpi/delete.post.ts 생성
2. 프론트에서 kpiId 보내면 해당 KPI 삭제
3. composables/useKpiApi.ts에 deleteKpi 함수 추가
4. 화면에 삭제 버튼 추가해서 테스트

힌트:

- 삭제: kpiList.filter(kpi => kpi.kpiId !== kpiId)
- 파라미터: kpiId
